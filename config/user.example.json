{
  "_comment": "Copy to ~/.config/mlb-report/user.json (or $MLB_REPORT_CONFIG_HOME/user.json). Never commit the real file.",
  "recipients": [
    { "name": "Graham MacAree", "email": "hi@grahammacaree.com" }
  ],
  "_send_when_quiet": "Email even when there is nothing to report. Off by default: the season ends in September, and the alternative is an empty digest every morning until April.",
  "send_when_quiet": false
}
